# ───────── Node Dependencies ───────────────
node_modules/
.pnp/
.pnp.js

# ───────── Production Builds ───────────────
dist/
build/

# ───────── Environment Files (Secrets) ─────
.env*
!.env.example
!.env.local.example

# ───────── Firebase & GCP Local Artifacts ──
.firebase/
firebase-debug.log*
firestore-debug.log*
functions/lib/
firebaserc

# ───────── Debug Logs ──────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
ui-debug.log*

# ───────── System Artifacts ────────────────
.DS_Store
.qodo
.bolt

# ───────── Local Scripts & Emulators ───────
firebaseEmulators.ts
schemadashboard1.tsx

# ───────── Coverage & Testing ──────────────
coverage/
*.lcov

# ───────── VS Code Remote Dev ──────────────
.vscode/
.history/
*.code-workspace

# ───────── Firebase Hosting & Rules (compiled only) ─────
firebase.json
database.rules.json
storage.rules

# ───────── GitHub & Workflows (if private CI) ───────────
.github/
# But keep markdowns:
!.github/**/*.md

# ───────── CSV/Excel/JSON Dumps ─────────────
*.csv
*.xls
*.xlsx
*.json
!src/**/*.json  # keep config files used in code
!package.json   # explicitly include package.json for builds
!package-lock.json  # include package-lock.json for dependency resolution

# ───────── Build Output for Functions ───────
functions/node_modules/
functions/lib/
functions/.tsbuildinfo

# Local Netlify folder
.netlify

*storybook.log
storybook-static
.vercel
chalk/
glob/
test-results/
# Ignore sensitive configuration files
client_secret_*.json
**/google-services.json
.env*
!.env.example
*.keystore
*.jks

ENV_SETUP_GUIDE.md
